"""
- `n::Int`: the first number
- `other_number::Int`: the second number
"""
function simple(n::Int, other_number::Int)
    # TODO Just do what you want with these numbers, like sum them.
end

n = parse(Int, readline())
other_number = parse(Int, readline())
simple(n, other_number)
