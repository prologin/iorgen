-- n: the first number
-- other_number: the second number
function simple(n, other_number)
    -- TODO Just do what you want with these numbers, like sum them.
end

local n = tonumber(io.read())
local other_number = tonumber(io.read())

simple(n, other_number)
