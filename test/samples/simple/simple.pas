program Simple;

{ @param N the first number }
{ @param OtherNumber the second number }
procedure Simple(N: longint; OtherNumber: longint);
begin
    {* TODO Just do what you want with these numbers, like sum them. *}
end;

var
    N: longint; { the first number }
    OtherNumber: longint; { the second number }
begin
    readln(N);
    readln(OtherNumber);
    Simple(N, OtherNumber);
end.
